"""
Stream2Vec — Backend Application Package.

This package contains the FastAPI application for the Stream2Vec platform.

Modules:
    main: Application entry point and FastAPI instance
    api: REST API routes and endpoints
    core: Core configuration, logging, and security
    database: Database sessions and connection management
    models: SQLAlchemy ORM models
    schemas: Pydantic request/response schemas
    repositories: Data access layer (Repository pattern)
    services: Business logic layer
    storage: MinIO object storage client
    messaging: Kafka messaging client
    dependencies: FastAPI dependency injection
    middlewares: HTTP middlewares
    exceptions: Custom exception handlers
    utils: Shared utilities
"""
