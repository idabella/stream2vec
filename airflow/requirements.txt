# =============================================================================
# Stream2Vec Airflow — extra Python deps (Airflow itself comes from the image)
# =============================================================================

# Extra deps only. Do not install qdrant-client here: its grpcio-tools
# pulls protobuf 7.x and breaks Airflow's Google providers.
kafka-python==2.0.2

