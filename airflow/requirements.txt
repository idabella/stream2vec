# =============================================================================
# Stream2Vec Airflow — Python Dependencies
# =============================================================================

apache-airflow==2.9.1
apache-airflow-providers-postgres==5.10.2
apache-airflow-providers-apache-kafka==1.3.1
apache-airflow-providers-amazon==8.19.0   # For MinIO (S3 compatible)

# --- Utilities ---
python-dotenv==1.0.1
